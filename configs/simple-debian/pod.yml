apiVersion: v1
kind: Pod
metadata:
  name: debian
spec:
  containers:
  - name: debian
    image: debian:bookworm
    command: [ "/bin/bash", "-c", "--" ]
    args: [ "while true; do sleep 9999999; done;" ]
